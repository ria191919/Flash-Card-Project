<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.flash_card_application">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashCardApplication"
        tools:targetApi="31">

        <!-- SplashActivity is the launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity that will be launched after SplashActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- AddFlashcardActivity that handles adding a new flashcard -->
        <activity
            android:name=".AddFlashcardActivity"
            android:exported="true">
        </activity>

        <!-- EditFlashcardActivity to handle editing flashcards -->
        <activity
            android:name=".EditFlashcardActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".FlashcardViewActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>
